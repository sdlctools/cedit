"""Vendored parsing core from the markdown-localization research repo.

`utils` is the pinned parser configuration; `tree_diff` the Merkle-hash
segmentation and similarity engine. Everything in `.cedit/` state is keyed
by these — treat this package as frozen and upgrade it only together with
the pins in `requirements.txt`.
"""
