# Release flow manual test

Dummy change used to exercise the gitflow pipeline end to end:
feature -> development -> release/sprint-* -> main, with a manually
minted version tag, to verify `release.yml`'s trigger behavior after
the default-branch fix.
