# CLAUDE.md

The project instructions live in [AGENTS.md](AGENTS.md), the assistant-neutral
convention, so that every AI tool reads the same file instead of one per
vendor. This file only pulls it in — put new instructions in `AGENTS.md`.

@AGENTS.md
